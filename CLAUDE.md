See [AGENTS.md](./AGENTS.md) for the project layout, stack, commands, and conventions.

Quick orientation:

- Frontend (Next.js) lives at the repo **root** (`src/app`, `src/components`, `src/lib`).
- Smart contracts (Foundry) live under **`contracts/`**.
- Design system is **`@breadcoop/ui`**; prefer its components and tokens.
