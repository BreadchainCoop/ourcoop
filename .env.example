ETH_RPC_URL=https://rpc.gnosis.gateway.fm
# ETH_BLOCK_NUMBER is optional - if not provided, tests will use the latest block
# ETH_BLOCK_NUMBER=32661487

# Cross-chain vote relays (comma-separated base URLs). Empty = no relay: voters
# self-submit castCrossChainVote from their wallet and share the signed payload.
NEXT_PUBLIC_RELAY_URLS=
